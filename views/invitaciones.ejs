<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invitaciones Pendientes</title>
    <link rel="stylesheet" href="/public/css/index.css"> <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <header>
        <nav class="nav-menu show">
            <a href="/home">Volver al Inicio</a>
        </nav>
    </header>

    <main class="main-container">
        <h1 class="main-title">Solicitudes de Acceso</h1>
        
        <% if (usuariosPendientes && usuariosPendientes.length > 0) { %>
            <div class="clientes-container">
                <% usuariosPendientes.forEach(u => { %>
                    <div class="cliente-card">
                        <p><strong>Nombre:</strong> <%= u.nombre %></p>
                        <p><strong>Email:</strong> <%= u.gmail %></p>
                        <div style="display: flex; gap: 10px; margin-top: 10px;">
                            <form action="/admin/aceptar-usuario/<%= u.id %>" method="POST">
                                <button type="submit" style="background-color: #28a745; color: white; border: none; padding: 8px 15px; cursor: pointer; border-radius: 5px;">Aceptar</button>
                            </form>
                            <form action="/admin/rechazar-usuario/<%= u.id %>" method="POST">
                                <button type="submit" style="background-color: #dc3545; color: white; border: none; padding: 8px 15px; cursor: pointer; border-radius: 5px;">Rechazar</button>
                            </form>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p class="no-clientes">No hay invitaciones pendientes por ahora.</p>
        <% } %>
    </main>
</body>
</html>